<%= simple_form_for(@account) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.association :top_account %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

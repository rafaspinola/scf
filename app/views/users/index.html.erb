<div class="container">
  <div class="row">
    <h3>Users</h3>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Perfil</th>
            <th>&nbsp;</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <%= render user %>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= link_to 'Novo Usuário', new_user_invitation_path %>
  </div>
</div>
